if (process.env.NODE_ENV !== "production") {
  require("dotenv").config();
}

const app = require("../app");
const port = 3000;

app.listen(port, () => {
  console.log(`Now you're listening to port ${port}`);
});
